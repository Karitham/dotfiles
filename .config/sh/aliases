# Kubectl
alias -g k="kubectl"
alias -g kply="kubectl apply -f"

# Ls
alias -g ll="ls --color=auto -lha"

# Cat
alias -g cat='bat -pp'

# Kitty
alias ssh='kitty +kitten ssh'

# Paths
alias ..='cd ..'
alias ...='cd ../..'

# Go -> Gottip
alias g="gotip "
alias gr="g run "
alias gt="g test "

# Zig
alias zbt="zig build test"
alias zbr="zig build run"

# Git
alias gcp="git add -A && git commit -S && git push"
