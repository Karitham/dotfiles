# Kubectl
alias -g k="kubectl"
alias -g kply="kubectl apply -f"
alias -g krr="kubectl rollout restart"

# Ls
alias -g ll="ls --color=auto -lha"

# Cat
alias -g cat=bat

# Kitty
ssh='kitty +kitten ssh'
